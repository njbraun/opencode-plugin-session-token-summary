# Security Policy

Report vulnerabilities privately through GitHub's security advisory workflow
for this repository. Do not include sensitive session content in public issues.

This plugin reads session metadata and messages through the local OpenCode API
solely to render sidebar usage totals. It does not transmit session data to
external services.
